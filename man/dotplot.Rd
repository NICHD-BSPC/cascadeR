% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{dotplot}
\alias{dotplot}
\title{Dotplot}
\usage{
dotplot(
  df,
  xcol,
  ycol,
  split = NULL,
  scale = TRUE,
  dot.scale = 10,
  col.min = -2.5,
  col.max = 2.5
)
}
\arguments{
\item{df}{data.frame with gene expression data}

\item{xcol}{x-axis grouping variable}

\item{ycol}{genes to show (on y-axis). can be multiple}

\item{split}{faceting variable}

\item{scale}{should data be scaled? Default: TRUE}

\item{dot.scale}{dot size scaling factor (default: 10)}

\item{col.min}{if data is scaled, this is the lower limit of values (default: -2.5)}

\item{col.max}{if data is scaled, this is the upper limit of values (default: 2.5)}
}
\value{
ggplot2 handle
}
\description{
Lightweight dotplot reimplementation to run only on data frame with
gene expression and metadata.
}
\examples{
df <- data.frame(
  cluster = factor(rep(c("A", "B"), each = 4)),
  condition = factor(rep(c("ctrl", "stim"), times = 4)),
  GeneA = c(0, 2, 4, 5, 1, 3, 6, 8),
  GeneB = c(1, 0, 3, 2, 5, 0, 7, 4)
)

dotplot(df, xcol = "cluster", ycol = c("GeneA", "GeneB"), scale = FALSE)
dotplot(df, xcol = "cluster", ycol = "GeneA", split = "condition")

}
