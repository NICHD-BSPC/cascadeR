# Get path to access yaml

This function checks for an environment variable 'CASCADE_ACCESS_YAML'
to specify directory containing access yaml. If env variable does not
exist uses home directory as save location.

## Usage

``` r
get_access_path()
```

## Value

path to access yaml
