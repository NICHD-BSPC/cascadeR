# Get plotly label trace

Plotly trace to add labels to existing plot.

## Usage

``` r
get_label_trace(plot_data, labeled_pts, split = FALSE)
```

## Arguments

- plot_data:

  plotly plot data

- labeled_pts:

  points to label

- split:

  column by which plot is split

## Value

plotly handle
